<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mini Site</title>
  <style>
    /* Genel stil ve animasyon */
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 50px;
      transition: background-color 0.5s;
    }

    h1 {
      font-size: 3em;
      margin-bottom: 20px;
      animation: fadeIn 1s ease-in;
    }

    @keyframes fadeIn {
      0% {opacity: 0;}
      100% {opacity: 1;}
    }

    /* Butonlar */
    .btn {
      padding: 15px 30px;
      margin: 10px;
      font-size: 1.2em;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .btn:hover {
      transform: scale(1.1);
      box-shadow: 0 0 20px rgba(0,0,0,0.3);
    }

    /* Giriş ve Kayıt alanı */
    input[type="email"], input[type="password"] {
      padding: 10px;
      margin: 10px;
      font-size: 1em;
      border-radius: 5px;
      border: 1px solid #ccc;
    }

    /* Tema ikon */
    .theme-selector {
      font-size: 2em;
      cursor: pointer;
      margin: 20px;
      display: inline-block;
      transition: transform 0.3s;
    }

    .theme-selector:hover {
      transform: rotate(20deg);
    }

    /* Soru işareti */
    .help {
      font-size: 2em;
      cursor: pointer;
      margin: 20px;
      display: inline-block;
      transition: transform 0.3s;
    }

    .help:hover {
      transform: rotate(20deg);
    }
  </style>
</head>
<body>

  <h1>Mini Site</h1>

  <!-- Email ve Şifre -->
  <input type="email" id="email" placeholder="E-posta"><br>
  <input type="password" id="password" placeholder="Şifre"><br>

  <!-- Butonlar -->
  <button class="btn" style="background-color:#ff6f61; color:white;" onclick="alert('Giriş yapıldı!')">Giriş Yap</button>
  <button class="btn" style="background-color:#6b5b95; color:white;" onclick="alert('Kayıt tamam!')">Kayıt Ol</button>

  <br>

  <!-- Tema değiştirme -->
  <span class="theme-selector" onclick="changeTheme()">🎨 Tema</span>

  <!-- Yardım butonu -->
  <span class="help" onclick="alert('Bu Site Şuan Yapılıyor!')">❓</span>

  <script>
    // Tema seçenekleri
    const themes = ['#fff176', '#ffffff', '#64b5f6', '#f48fb1', '#81c784', '#000000'];
    let currentTheme = 0;

    function changeTheme() {
      currentTheme = (currentTheme + 1) % themes.length;
      document.body.style.backgroundColor = themes[currentTheme];
    }
  </script>

</body>
</html>